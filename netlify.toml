[build]
  publish = "public"
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

# Sur Netlify, on laisse les fichiers HTML être servis normalement
# L'authentification est gérée par check-auth dans le frontend
